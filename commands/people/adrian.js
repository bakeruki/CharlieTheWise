module.exports = {
    name: "adrian",
    category: "people",
    description: "Gives a description of Adrian",
    permissions: [],
    devOnly: false,
    run: async ({client, message, args}) => {
        message.reply(`Adrian is many things.. a simp.. a gamer.. he's a jack of all trades that should not be messed with.`)
    }
}